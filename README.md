Currently simple CPP program to create an OpenGL window. The program uses CMake to compile.
Procedure to make (windows):

  -> mkdir build
  
  -> cd build
  
  -> cmake ..
  
  -> cmake --build . --config Release
  
  -> Release/opengl_thing.exe
